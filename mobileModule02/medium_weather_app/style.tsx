import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text_route: {
    fontSize: 24,
    textAlign: 'center',
    fontWeight: 'bold',
  },
  cityText: {
    fontSize: 18,
    marginTop: 10,
    color: '#666',
  }
});